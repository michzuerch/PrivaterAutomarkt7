{
  "compilerOptions": {
    "baseUrl": ".", // This must be specified if "paths" is.
    "paths": {
      "@testhelper/*": ["packages/testhelper/*"] // This mapping is relative to "baseUrl".
    }
  }
}
